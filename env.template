PORT=5000
JWT_SECRET=change_this_to_a_random_secret_key_in_production
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Copy this file to .env and update the values
# For production, use a strong random string for JWT_SECRET
# Generate a secure JWT_SECRET: openssl rand -base64 32
# FRONTEND_URL should be your production frontend URL in production

